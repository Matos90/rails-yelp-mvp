<h1>Reviews</h1>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: 1..5 %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
<%= link_to "back", new_restaurant_path, class: "btn btn-warning"%>
